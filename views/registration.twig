<!DOCTYPE html>
<html>
    <head>
        <title>Registration</title>
        <link href="/assets/bower/bootstrap/dist/css/bootstrap.min.css" rel="stylesheet" type="text/css"/>
    </head>
    <body>
        <div class="container">
            <div class="row">
                <div class="col-lg-12">
                    <h4>New Registration</h4>
                    {% for message in app.session.getFlashBag.get('alert_danger') %}
                        <div class="alert alert-danger" align="center">
                            <p>{{message}}</p>
                        </div>
                    {%endfor%}
                </div>
                <form action = "/register" method = "post" enctype="multipart/form-data">
                    <div class="col-lg-6">
                        <label>Name:</label>
                        <input type  = "text" name = "userName" class='form-control' required>
                        <label>Email Id:</label>
                        <input type  = "email" name = "userEmail"  class='form-control'  required="required">
                        <label>Password:</label>
                        <input type  = "password" name = "userPassword" class='form-control'  required="required">
                        <label>Upload resumé:</label>
                        <span style="color:grey"> allowed file types: PDF, DOC & DOCX </span><br>
                        <input type='file' name='resumeFile' id='resumeFile' class="form-control" required><br>
                    </div>
                    <div class="col-lg-6">
                        <label>Office Location:</label>
                        <select id="location" name="officeLocation" class="form-control">
                            <option value=''>Select</option>
                            <option value='Kolkata Sector V'>Kolkata Sector V</option>
                            <option value='Kolkata Kestopur'>Kolkata Kestopur</option>
                            <option value='Chennai'>Chennai</option>
                        </select>
                        <label>Address:</label>
                        <textarea id="userAddress" name="userAddress" class="form-control" rows="6">{% if userData.user_address is defined %}{{userData.user_address}}{% endif %}</textarea><br>
                        <button type = "submit" name = "New Registration" value = "Sign Up" class='btn btn-primary btn-block'>Register</button>

                    </div>
                    <div class="col-lg-12">
                        <a href='/login' class='btn btn-default btn-general'>Already User? Login</a>
                    </div>
                </form>
            </div>
        </div>
    </body>
</html>